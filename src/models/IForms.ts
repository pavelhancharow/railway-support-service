export interface IForm {
  from: string;
  to: string;
  train: string;
}

export interface ILogin {
  login: string;
  password: string;
}

export interface IAddRoute {
  from: string;
  to: string;
  distance: string;
}
